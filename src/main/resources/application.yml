server:
  port: 8080
spring:
  datasource:
    url: jdbc:postgresql://localhost:5342/todo
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
